/** APP 更新模块常量 */

/** APK 专用资源根目录 ID（resources.json 中幂等注册） */
export const APK_ROOT_ID = 'apk';

/** APK 专用资源根的默认相对路径（与 resources 目录同级） */
export const APK_ROOT_PATH = 'apk';

/** 环境变量：外部可访问的基础地址（可选，优先于请求头动态拼接） */
export const ENV_PUBLIC_BASE_URL = 'APP_PUBLIC_BASE_URL';
